# 📝 Programming Challenges

本倉庫紀錄三道程式題目的詳細描述與解法，包括 **Above Average**、**Odd Sum without Prime** 和 **Prime Factor**。

---

## 📌 1. Above Average
據說，90% 的大學一年級新生期望他們自己的成績能在全班平均之上，請你來幫忙驗證看看他們有沒有達成目標。

### 📥 Input
- 第一行輸入一個整數 `C`，代表有 `C` 組測試資料。
- 每組測試資料：
  - 第一個整數 `N` 代表班級總人數，`1 <= N <= 1000`。
  - 接下來的 `N` 個整數代表每位學生的期末總成績，`0 <= 成績 <= 100`。

### 📤 Output
對每組測試資料輸出一行，計算有多少百分比的學生成績高於全班平均，**四捨五入到小數點後第三位**。

### 🔹 Sample Input / Output
```plaintext
Input:
5
5 50 50 70 80 100
7 100 95 90 80 70 60 50
3 70 90 80
3 70 90 81
9 100 99 98 97 96 95 94 93 91

Output:
40.000%
57.143%
33.333%
66.667%
55.556%

# 📌 Odd Sum without Prime

本題要求計算 **範圍內所有奇數但不是質數的總和**。

## 📥 Input
- 第一行輸入一個整數 `N`，代表有 `N` 組測試資料。
- 接下來的 `N` 行，每行包含兩個正整數 `a` 和 `b`，且 `2 < a <= b < 100`。

## 📤 Output
- 對每組測試資料輸出一個整數，表示範圍 `[a, b]` 內所有 **奇數但不是質數** 的總和。

## 🔹 Sample Input / Output
```plaintext
Input:
2
3 16
10 20

Output:
24
15

# 📌 Prime Factor (質因數分解)

本題要求將輸入的正整數 `n` 轉換為 **質因數乘積表示法**，  
若某個質因數出現多次，則以 **次方表示**。

## 📥 Input
- 輸入一個正整數 `n`。

## 📤 Output
- 輸出 `n` 的質因數分解結果，以 `a^b` 形式表示質因數的次方。

## 🔹 Sample Input / Output
```plaintext
Input:
240

Output:
240=2^4*3*5
